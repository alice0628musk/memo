javascript 시작
파이어썬, iso, 자바스크립트는 모두 영어,중국어,일본어,한국어처럼 언어의 한종류이며 가장 많이 쓰는 언어 중 하나이다.

-dash-case(kebab-case): css에서 자주 사용하고
-snakecase : 폴더 이름 지을때 자주사용
-camelCase: 자바스크립트에서 쓰는 사용이름



 console.log("email"); 쌍따옴표를 넣으면 변수가 아니라 문자 그대로 출력된다. 문자데이터가된다 "문자야"

`백틱 기호 1번 앞에 있는 키로 쓴다.

    let myName = "alice";
    let hello = `안녕! ${myName}야`;
${변수이름} -> 변수 출력 방법

숫자 데이터는 ""안넣어도 된다

isValid: true - 뭔지는 모르지만 어떤 값을 트루로 가지고 있다.