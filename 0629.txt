<코드팬>
+미리 테스트해볼수 있는 메모장같은거
https://codepen.io/
자주쓰는 코드등을 저장해두고 두고두고 긁어 사용할수 있는 곳
남의것도 가져올수 있고 깃헙처럼 포크로 찍어서 저장해둘수도있다.

CSS Base
 Normalize
 Reset
 Neither
설정에 Reset 기본 코딩값의 css를 리셋해줌

+박스 4개 나열하는데 옛날처럼 px로 채워서 넣지 않는다
flex를 사용해 반응형으로 늘어나야하는데
플랙서블flexerble하지 않는 코드를 요즘 쓰지않는다
디바이스가 많아서 요즘은 그렇게 안된다.


+scss 사스 코드로 쓰면 마더값이 없어도 각각 찢어서 코딩을 먹일 수 있다
사스 코딩을 할줄 알면 그 분야 취업이 따로 가능하다.

+calc 칼크 함수를 쓰면  width: calc(100% - 100px); 알아서 비율대비 계산해줘서 알아서 크기를 정하고 자리를 잡는다

+position: absolute; 포지션 코드를 쓰면 
https://www.w3schools.com/css/tryit.asp?filename=trycss_position_absolute
메인에 전체 곽을 이걸로 코딩하면 안된다! 페이지가 느려진다.

포지션은 그냥 옛날 플로팅배너같은 공중에 띄워주는 작은 배너에서만 쓴다

메인 페이지 곽은 지금은 flex코딩을 쓴다.
